# Core requirements
aiohttp>=3.9.0
aiortc>=1.6.0
ultralytics>=8.0.0
pyyaml>=6.0
aioice>=0.9.0
av>=10.0.0

# GPU requirements (will install CPU versions if GPU not available)
torch>=2.0.0  # Auto-detects CUDA
torchvision>=0.15.0
onnxruntime-gpu>=1.15.0  # GPU version of ONNX Runtime

# Vision
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0